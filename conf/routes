# Routes

# Store User Details
POST           /sign-up/store-user-details/:requestId         controllers.StoreUserDetailsController.storeUserDetails(requestId)

# Retrieve Login Details
POST           /sign-up/check-login-details/:requestId        controllers.CheckLoginDetailsController.checkLoginDetails(requestId)